<?php  
    $name = $_POST [ "name" ]; //Empfange den Wert des Namensfelds in der Variable $name
    $mail = $_POST ["mail"]; //Empfange den Wert der Mail Adresse  in der Variable $mail
    $msg = $_POST ["msg"]; //Empfangen den Wert der Nachricht in der Variable $msg

    // echo "Willkommen: $name, Ihre Nachricht "$msg" ;   
    ?>  